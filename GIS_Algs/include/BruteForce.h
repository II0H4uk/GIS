#pragma once

#include "Graph.h"

namespace GIS_Algs {

    class BruteForce {
    private:

    public:
        static bool Start(const GIS_Data::Graph& graph1,const GIS_Data::Graph& graph2);
    };
}
